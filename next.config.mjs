/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['preproot-app.s3.ap-south-1.amazonaws.com', 'hebbkx1anhila5yf.public.blob.vercel-storage.com'],
    },
};

export default nextConfig;
