/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['preproot-app.s3.ap-south-1.amazonaws.com', 'https://www.edzy.ai'],
    },
};

export default nextConfig;
